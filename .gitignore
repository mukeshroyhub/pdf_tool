node_modules/
.next/
# Drop-in licensed fonts: server-side assets only, never committed (the files
# are proprietary — they go onto the host by hand, not through the repo).
fonts-custom/
apps/api/fonts/
dist/
*.tsbuildinfo
.env
.env.local
apps/api/*.db
apps/api/prisma/*.db
apps/api/prisma/*.db-journal
apps/api/uploads/
install.log
.DS_Store
